import { PincodedirectiveDirective } from './pincodedirective.directive';

describe('PincodedirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PincodedirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
