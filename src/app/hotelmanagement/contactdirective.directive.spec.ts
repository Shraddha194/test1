import { ContactdirectiveDirective } from './contactdirective.directive';

describe('ContactdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
